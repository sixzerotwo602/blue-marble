---
description: Notion 가이드(2d84a4ce...)를 준수하여 Maro Innovation Plan 대비 현재 코드베이스의 개발 진척도를 분석하고, Notion 현황판을 업데이트합니다.
---

1. **분석 기준 문서 확보 (Maro Innovation Plan)**
   - **원본 문서**: Notion 페이지 `2d84a4ce-0de2-8088-a82a-e1fc87ffe298` (Maro Innovation Plan)
   - **도구**: `mcp_notion-mcp-server_API-get-block-children`
   - 문서를 읽어 전체 기능 요구사항과 마일스톤을 파악합니다.

2. **코드베이스 정밀 분석**
   - **대상 경로**: `e:\github_coop\maro-bot`
   - **분석 방법**:
     - 요구사항의 각 기능별로 관련 파일(`find_by_name`) 및 핵심 로직(`grep_search`) 존재 여부를 확인합니다.
     - **슬래시 명령어 확인**: `.agent/workflows` 디렉토리 내의 워크플로우 파일들을 확인하여 구현된 명령어를 파악합니다.
   - **진행률 산정**:
     - `진행률 = (구현된 하위 기능 수 / 전체 하위 기능 수) × 100%`

3. **개발 현황 문서 생성 (Notion)**
   - **타겟 DB**: `2d84a4ce-0de2-80aa-bd91-fe7c8feb0eb9`
   - **페이지 제목**: `[현황] 개발 진척도 상세 리포트 - {YYYY/MM/DD}`
   - **문서 구조** (가이드 준수):
     1. **전체 진행 현황 요약**:
        - 총 진행률, 핵심 달성 사항, 현재 단계 요약.
     2. **상세 기능별 개발 현황**:
        - 각 마일스톤/기능별로 `완료 / 부분 구현 / 미구현` 상태를 명시합니다.
        - **구현된 경우**: 증거 파일 경로 또는 핵심 클래스명을 포함합니다.
        - **미구현된 경우**: 간단한 사유나 향후 계획을 메모합니다.
     3. **구현된 슬래시 명령어 목록**:
        - 현재 사용 가능한 `/command` 목록과 설명을 정리합니다.
     4. **다음 개발 우선순위 제안**:
        - 미구현 항목 중 가장 시급하거나 의존성 상 먼저 해야 할 작업을 제안합니다.

4. **리포트 발행 및 공유**
   - `mcp_notion-mcp-server_API-post-page`를 사용하여 페이지를 생성합니다.
   - 생성된 Notion 페이지 URL을 채팅창에 출력하여 사용자에게 알립니다.
